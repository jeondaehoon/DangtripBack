<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.freefit.freefitapp.signup.dao.signupDao">

    <resultMap id="signupMap" type="com.freefit.freefitapp.vo.userinfoVO">
        <result column="USERID" property="userId"/>
        <result column="PASSWORD" property="password"/>
        <result column="EMAIL" property="email"/>
        <result column="NAME" property="name"/>
        <result column="BIRTH" property="birth"/>
        <result column="CARRIER" property="carrier"/>
        <result column="GENDER" property="gender"/>
        <result column="NATIONALITY" property="nationality"/>
        <result column="PHONE" property="phone"/>
    </resultMap>

    <select id="checkId" resultType="int">
        SELECT COUNT(*)
        FROM USERINFO
        WHERE USERID = #{userId}
    </select>

    <insert id="signup" parameterType="userinfoVO">
        INSERT INTO userinfo (
            USERID, PASSWORD, EMAIL, NAME, BIRTH, CARRIER, GENDER, NATIONALITY, PHONE
        ) VALUES (
                     #{userId}, #{password}, #{email}, #{name},
                     TO_DATE(#{birth}, 'YYYY.MM.DD'),
                     #{carrier}, #{gender}, #{nationality}, #{phone}
                 )
    </insert>

</mapper>
